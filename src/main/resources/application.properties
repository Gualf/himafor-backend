# Konfigurasi Server
server.port=${PORT:8080}
server.servlet.context-path=/api/v1

# Konfigurasi Nama Aplikasi
spring.application.name=himafor

# Konfigurasi Database Supabase PostgreSQL
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.yywpabmofygfgyxyxggn}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:anakbaik11122}
spring.datasource.driver-class-name=org.postgresql.Driver

# Konfigurasi JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Konfigurasi JWT Security
app.jwt.secret=${APP_JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration-ms=86400000

# Konfigurasi File Storage
file.upload-dir=uploads
